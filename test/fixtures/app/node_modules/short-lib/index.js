// Only four exports — below the count threshold — but all single-letter, so
// the report should still collapse them via the "looks minified" heuristic.
export const a = 1;
export const b = 2;
export const c = 3;
export const d = 4;
